# Runtime mode defaults
DEFAULT_MODE=local-only
DEFAULT_TIER=low

# App
APP_ENV=development
LOG_LEVEL=INFO

# Optional external adapters (empty by default)
TAVILY_API_KEY=
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=

# Local DB path
SQLITE_PATH=db/aideator.db
